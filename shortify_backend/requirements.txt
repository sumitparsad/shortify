# Web Framework
fastapi==0.138.1
uvicorn[standard]==0.34.3

# Database
sqlalchemy==2.0.51
asyncpg>=0.31.0
alembic==1.18.5

# Redis
redis==5.2.1

# Auth
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20

# Pydantic
pydantic==2.13.4
pydantic-settings==2.9.1

# NanoID
nanoid==2.0.0

# HTTP Client (user-agent parsing)
user-agents==2.2.0

# Logging
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
pytest-cov==6.1.0
anyio==4.9.0

# Dev
python-dotenv==1.1.0

# Email validation (required by pydantic EmailStr)
email-validator==2.2.0

# Async SQLite driver (for tests only)
aiosqlite==0.20.0

# PostgreSQL sync driver (for Alembic migrations)
psycopg[binary]>=3.2.0